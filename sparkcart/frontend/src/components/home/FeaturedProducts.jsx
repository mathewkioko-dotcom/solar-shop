import { useEffect, useMemo, useState } from 'react'
import { Link } from 'react-router-dom'
import solarPanelsIcon from '../../assets/icons/categories/solar-panels.svg'
import shoppingBagIcon from '../../assets/icons/ecommerce/shopping-bag.svg'
import starIcon from '../../assets/icons/ecommerce/star.svg'
import wishlistIcon from '../../assets/icons/ecommerce/wishlist.svg'
import arrowRightIcon from '../../assets/icons/navigation/arrow-right.svg'
import { getProductImageUrl } from '../../services/productService'
import SvgIcon from '../ui/SvgIcon'

const API_URL = 'http://localhost:8000/api/products'

const formatPrice = (value) => {
  const number = Number(value)
  return Number.isNaN(number) ? 'Contact for price' : new Intl.NumberFormat('en-KE', { style: 'currency', currency: 'KES', maximumFractionDigits: 0 }).format(number)
}
const normalizeProducts = (response) => Array.isArray(response) ? response : Array.isArray(response?.data) ? response.data : Array.isArray(response?.products) ? response.products : []

function FeaturedProducts({ searchTerm }) {
  const [products, setProducts] = useState([]); const [loadingProducts, setLoadingProducts] = useState(true); const [productError, setProductError] = useState('')
  useEffect(() => { const controller = new AbortController(); async function loadProducts() { try { setLoadingProducts(true); setProductError(''); const response = await fetch(API_URL, { signal: controller.signal, headers: { Accept: 'application/json' } }); if (!response.ok) throw new Error(`Products request failed: ${response.status}`); setProducts(normalizeProducts(await response.json())) } catch (error) { if (error.name !== 'AbortError') { console.error(error); setProductError('Products could not be loaded. Confirm that the Laravel backend is running.') } } finally { setLoadingProducts(false) } } loadProducts(); return () => controller.abort() }, [])
  const featuredProducts = useMemo(() => { const query = searchTerm.trim().toLowerCase(); const filtered = !query ? products : products.filter((product) => { const category = typeof product.category === 'object' ? product.category?.name : product.category; return [product.name, product.description, product.brand, category, product.slug].filter(Boolean).some((value) => String(value).toLowerCase().includes(query)) }); return filtered.slice(0, 8) }, [products, searchTerm])
  return <section id="featured-products" className="products-section"><div className="container"><div className="section-heading heading-with-action"><div><p>Featured products</p><h2>{searchTerm ? `Search results for “${searchTerm}”` : 'Popular solar products'}</h2><span>Carefully selected products from trusted manufacturers.</span></div><button className="text-button" type="button">View all products <SvgIcon src={arrowRightIcon} size={16} /></button></div>{loadingProducts && <div className="products-grid">{Array.from({ length: 8 }).map((_, index) => <div className="product-skeleton" key={index}><div className="skeleton-image" /><div className="skeleton-line skeleton-line-short" /><div className="skeleton-line" /><div className="skeleton-line skeleton-line-medium" /></div>)}</div>}{!loadingProducts && productError && <div className="products-message error-message"><strong>Products are temporarily unavailable.</strong><span>{productError}</span></div>}{!loadingProducts && !productError && featuredProducts.length === 0 && <div className="products-message"><strong>No products found.</strong><span>Try searching with a different product name.</span></div>}{!loadingProducts && !productError && featuredProducts.length > 0 && <div className="products-grid">{featuredProducts.map((product) => { const image = getProductImageUrl(product); const category = typeof product.category === 'object' ? product.category?.name : product.category; const slug = typeof product.slug === 'string' ? product.slug.trim() : ''; const productPath = slug ? `/products/${encodeURIComponent(slug)}` : ''; return <article className="product-card" key={product.id}><div className="product-image-wrapper">{image && <img src={image} alt={product.name} loading="lazy" onError={(event) => { event.currentTarget.style.display = 'none'; const fallback = event.currentTarget.parentElement.querySelector('.product-image-fallback'); if (fallback) fallback.style.display = 'flex' }} />}<div className="product-image-fallback" style={{ display: image ? 'none' : 'flex' }}><img src={solarPanelsIcon} alt="" /><small>Baraka Solar Shop</small></div>{productPath && <Link to={productPath} aria-label={`View ${product.name}`} style={{ position: 'absolute', inset: 0 }}><span className="sr-only">View {product.name}</span></Link>}{Number(product.stock) > 0 && <span className="stock-badge">In stock</span>}<button className="wishlist-button" type="button" aria-label={`Add ${product.name} to wishlist`}><SvgIcon src={wishlistIcon} size={20} /></button></div><div className="product-card-content"><p className="product-category">{category || product.brand || 'Solar equipment'}</p><h3>{productPath ? <Link to={productPath}>{product.name}</Link> : product.name}</h3><div className="product-rating"><span>{Array.from({ length: 5 }).map((_, index) => <SvgIcon src={starIcon} size={13} key={index} />)}</span><small>5.0</small></div><div className="product-card-footer"><strong>{formatPrice(product.price)}</strong><button type="button" aria-label={`Add ${product.name} to cart`}><SvgIcon src={shoppingBagIcon} size={20} /></button></div></div></article> })}</div>}</div></section>
}
export default FeaturedProducts
